import { Button, Container } from '@nextui-org/react';
import React from 'react';

export const Landing: React.FC = () => {
  return (
    <Container>
      <Button>Hi</Button>
    </Container>
  );
};
